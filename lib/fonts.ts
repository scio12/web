import { Lato, Poppins, Play } from "@next/font/google";

export const lato = Lato({
  weight: ["400"],
  subsets: ["latin"],
});

export const poppins = Poppins({
  weight: ["400"],
  subsets: ["latin"],
});

export const play = Play({
  weight: ["400", "700"],
  subsets: ["latin"],
});
